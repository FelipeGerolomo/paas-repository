import { SettingsClient } from "@/components/settings-page"

export const metadata = {
  title: "Configurações - Deploy Dashboard",
}

export default function SettingsPage() {
  return <SettingsClient />
}
